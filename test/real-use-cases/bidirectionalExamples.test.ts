/**
 * Similar to multi-slave examples file, but with more options (conflict handling, etc),
 * realistic push/fetch (full-sync), doing several batches until full-sync is achieved, etc.
 */

describe("multi slave examples", () => {
  xtest("TODO", () => {

  });
});
